export const irParaInicio = (history) => {
    history.push('/')
    history.go(0)
}
export const irParaSobre = (history) => {
    history.push('/sobre')
    history.go(0)
}
export const irParaProjetos = (history) => {
    history.push('/projetos')
    history.go(0)
}